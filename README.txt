To use TMS, first install server/database_schema/task_list.sql on a MySQL server then edit src/tms/DBInfo.java to configure the database.
Run "mvn install assemebly:assembly" to generate a .jar file with dependencies from the client directory to build an executable jar

